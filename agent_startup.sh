#!/bin/bash

# Navigate to the project directory (if needed)
# cd /path/to/your/project

# Run the command
pnpm start --character="character/aixbt_agent.json"

# Optional: Keep the script running in the background
# nohup pnpm start --character="character/aixbt_agent.json" > output.log 2>&1 &
